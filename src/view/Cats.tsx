import { FC } from "react";
import { CatList, Layout } from "../components";

const Cats: FC = () => {
  return (
    <Layout>
      <CatList />
    </Layout>
  );
};

export default Cats;
